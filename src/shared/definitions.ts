export interface Constants {
    pathStartChar: string;
    pathEndChar: string;
    cross: string;
    horizontalPath: string;
    verticalPath: string;
    alphabet: string[];
}

export enum Direction {
    north = 'north',
    east = 'east',
    south = 'south',
    west = 'west'
}
